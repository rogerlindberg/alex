[project]
name = "alex"
version = "0.1.0"
description = "A lexer"
authors = [
    { name = "Roger" }
]
readme = "README.md"
requires-python = ">=3.13"

[project.urls]
Homepage = "https://example.com"

[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"
